apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nexo-raiden-postgresql
  namespace: argocd
spec:
  project: nexo-raiden-platform
  source:
    repoURL: https://github.com/nexoraiden/homelab-gitops.git
    targetRevision: main
    path: infrastructure/data/postgresql
  destination:
    server: https://kubernetes.default.svc
    namespace: data
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
