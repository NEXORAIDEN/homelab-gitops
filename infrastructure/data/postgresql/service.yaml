apiVersion: v1
kind: Service
metadata:
  name: nexo-raiden-postgresql
  namespace: data
spec:
  type: ClusterIP
  selector:
    app: nexo-raiden-postgresql
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
